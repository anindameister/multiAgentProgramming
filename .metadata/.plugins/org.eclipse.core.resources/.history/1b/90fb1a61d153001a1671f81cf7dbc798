mas tentative3 {
    agent bob { // file arunava.asl is used
      goals: say("Hello Beautiful, how are we doing today?")
    }
    agent alice { // file bela.asl is used
      beliefs: say("Yaad nahi kal tujhe jharu se mari this, abhi kya chahiye?")
   }
}